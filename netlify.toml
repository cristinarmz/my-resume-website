[build]
  command = "npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "22"

# Redirecciona todas las rutas a index.html (necesario para apps de React)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
